def calculate_exponent(num, exp):
    """
    Create a function that takes a base number and an 
    exponent number and returns the calculation.
    """
    result = num**exp
    return result


print(calculate_exponent(10, 3))
