def find_perimeter(length, width):
    """
    Find perimeter of a rectangle.
    """
    side1 = length * 2
    side2 = width * 2
    result = side1 + side2
    return result


print(find_perimeter(2, 4))
